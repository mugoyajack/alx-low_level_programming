#include "main.h"
/**
 * _abs - test the absolute value of an int
 * @n: integer value to test
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
